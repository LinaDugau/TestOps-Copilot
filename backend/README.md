# TestOps Copilot - Backend

Backend-часть приложения TestOps Copilot для автоматической генерации тестов с использованием Cloud.ru Evolution API.

## 📋 Описание

Backend предоставляет REST API для генерации тестов различных типов:
- **Ручные UI тесты** - тест-кейсы для UI калькулятора
- **Ручные API тесты** - тест-кейсы для Evolution Compute API
- **Автоматические UI тесты** - e2e тесты на Playwright + pytest
- **Автоматические API тесты** - автоматизированные тесты на pytest + requests с использованием OpenAPI спецификации
- **Генератор тест-плана** - создание структурированного тест-плана
- **Оптимизация тестов** - анализ и оптимизация существующих тестов
- **Анализ дефектов GitLab** - загрузка исторических багов для учёта в оптимизации
- **Unit-тесты для CI/CD** - быстрые pytest тесты для FastAPI эндпоинтов + .gitlab-ci.yml

## 🚀 Установка и запуск

### Требования

- Python 3.11+
- API ключ Cloud.ru (Bearer token)
- GitLab токен (опционально, для коммита тестов)

### Шаг 1: Установка зависимостей

```bash
cd backend

# Создание виртуального окружения (если еще не создано)
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` в папке `backend`:

```env
CLOUD_RU_API_KEY=your_api_key_here
CLOUD_RU_MODEL=Qwen/Qwen3-Next-80B-A3B-Instruct

# Опционально: для коммита тестов в GitLab
GITLAB_URL=https://gitlab.com
GITLAB_TOKEN=your_gitlab_token_here
```

Где получить API ключ:
1. Зайдите в [Cloud.ru Console](https://console.cloud.ru/)
2. Перейдите в раздел "API Keys"
3. Создайте новый Bearer token
4. Скопируйте ключ в `.env` файл

Где получить GitLab токен (для коммита тестов):
1. Зайдите в GitLab
2. Перейдите в Settings → Access Tokens
3. Создайте токен с правами `api` и `write_repository`
4. Скопируйте токен в `.env` файл
5. Укажите `GITLAB_URL` (например, `https://gitlab.com` или URL вашего GitLab instance)

### Шаг 3: Запуск сервера

```bash
# Убедитесь, что виртуальное окружение активировано
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Сервер будет доступен по адресу: `http://localhost:8000`

### Шаг 4: Проверка работы

Откройте в браузере или выполните запрос:

```bash
curl http://localhost:8000/
```

Должен вернуться ответ:
```json
{
  "message": "TestOps Copilot работает на 100%. Все режимы — включая оптимизацию и тест-план — готовы к защите!"
}
```

Документация API доступна по адресу: `http://localhost:8000/docs` (Swagger UI)

## 📡 API Endpoints

### POST `/generate`

Генерирует тесты или анализирует существующий код в зависимости от типа запроса.

#### Request Body

```json
{
  "type": "string",           // Тип генерации (обязательно)
  "previous_code": "string",  // Предыдущий код для оптимизации/тест-плана (опционально)
  "repo_id": "string"         // ID или путь репозитория GitLab для загрузки багов (опционально, только для optimize)
}
```

#### Параметры

- **type** (обязательно): Тип генерации. Доступные значения:
  - `"manual_ui"` - Ручные UI тесты для калькулятора (18+ тест-кейсов)
  - `"manual_api"` - Ручные API тесты для Evolution Compute (18+ тест-кейсов)
  - `"auto_ui"` - Автоматические e2e UI тесты (Playwright + pytest)
  - `"auto_api"` - Автоматические API тесты (pytest + requests) с использованием OpenAPI спецификации
  - `"test_plan"` - Генерация тест-плана на основе предыдущего кода
  - `"optimize"` - Оптимизация существующих тестов (удаление дублей, улучшение покрытия)
  - `"unit_ci"` - Unit-тесты для FastAPI + .gitlab-ci.yml для CI/CD

- **previous_code** (опционально): Используется для режимов `test_plan` и `optimize`. Содержит код, который нужно проанализировать или оптимизировать.
- **repo_id** (опционально): Для режима `optimize` — ID или путь GitLab репозитория, откуда будут загружены issues с label `bug` / `defect` и учтены в анализе.

#### Response

```json
{
  "code": "string",           // Сгенерированный/оптимизированный код
  "validation": {             // Результаты валидации
    "valid": true,            // Успешна ли валидация
    "issues": [],             // Список найденных проблем
    "message": "string",      // Сообщение о валидации
    "score": 100              // Оценка качества (0-100)
  },
  "type": "string",           // Тип запроса
  "raw_length": 1234,         // Длина сырого ответа от LLM
  "clean_length": 1200        // Длина очищенного кода
}
```

#### Примеры запросов

**Генерация автоматических API тестов:**
```bash
curl -X POST "http://localhost:8000/generate" \
  -H "Content-Type: application/json" \
  -d '{"type": "auto_api"}'
```

**Оптимизация существующих тестов:**
```bash
curl -X POST "http://localhost:8000/generate" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "optimize",
    "previous_code": "import pytest\nimport requests\n\n@pytest.mark.test\ndef test_example():\n    assert True"
  }'
```

**Генерация тест-плана:**
```bash
curl -X POST "http://localhost:8000/generate" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "test_plan",
    "previous_code": "import pytest\n..."
  }'
```

#### Особенности работы

1. **Режим `auto_api`**:
   - Автоматически загружает OpenAPI спецификацию из `openapi/openapi-v3.yaml`
   - Извлекает все эндпоинты, схемы и параметры
   - Генерирует тесты с учетом реальной спецификации API
   - Проверяет покрытие всех эндпоинтов

2. **Очистка кода**:
   - Удаляет markdown форматирование (```python, ```)
   - Убирает типичные фразы LLM модели
   - Исправляет незакрытые строки
   - Валидирует синтаксис Python

3. **Валидация**:
   - Проверяет соответствие стандартам Allure
   - Для `test_plan` и `optimize` валидация не выполняется (возвращается успешный статус)

4. **Оптимизация с учётом багов**:
   - Для `optimize` можно передать `repo_id`, чтобы подставить исторические баги из GitLab (`/analyze_defects`) в промпт через плейсхолдеры `{defects_summary}` / `{historical_bugs}`.

### POST `/commit`

Коммитит сгенерированный код в GitLab репозиторий.

#### Request Body

```json
{
  "repo_id": 12345,                    // ID репозитория в GitLab (обязательно)
  "branch": "main",                    // Название ветки (по умолчанию: "main")
  "file_path": "tests/generated_tests.py",  // Путь к файлу в репозитории (обязательно)
  "commit_message": "Generated tests from TestOps Copilot",  // Сообщение коммита
  "code": "import pytest\n..."         // Сгенерированный код для коммита (обязательно)
}
```

#### Response

**Успешный ответ:**
```json
{
  "message": "Файл tests/generated_tests.py успешно создан в ветке main",
  "commit_sha": "abc123def456..."
}
```

**Ошибка:**
```json
{
  "detail": "GitLab не настроен. Укажите GITLAB_URL и GITLAB_TOKEN в .env файле"
}
```

#### Пример запроса

```bash
curl -X POST "http://localhost:8000/commit" \
  -H "Content-Type: application/json" \
  -d '{
    "repo_id": 12345,
    "branch": "main",
    "file_path": "tests/manual_ui_tests.py",
    "commit_message": "Add generated UI tests",
    "code": "import allure\n..."
  }'
```

#### Особенности работы

- Если файл существует, он будет обновлён
- Если файл не существует, он будет создан
- Если ветка не существует, она будет создана на основе default branch проекта
- Требует настройки `GITLAB_URL` и `GITLAB_TOKEN` в `.env` файле

### POST `/analyze_defects`

Загружает исторические дефекты (issues) из GitLab и, при необходимости, агрегирует их через LLM для подстановки в промпт оптимизации.

#### Request Body

```json
{
  "repo_id": "group/project-or-id",  // ID или путь репозитория (обязательно)
  "labels": ["bug"],                 // Фильтр по labels (по умолчанию: ['bug'])
  "state": "opened",                 // 'opened' | 'closed' | 'all'
  "max_issues": 10,                  // Лимит загружаемых issues (0 = без лимита)
  "summarize": true                  // Если true — вернёт краткое LLM summary
}
```

#### Response

```json
{
  "count": 3,
  "summary": "LLM summary of bugs...",
  "defects": [
    { "id": 123, "title": "Bug title", "description": "...", "labels": ["bug"], "state": "opened", "created_at": "2024-01-01T00:00:00Z" }
  ],
  "recommendations": "Интегрируй в тесты: частые баги по labels"
}
```

#### Пример запроса

```bash
curl -X POST "http://localhost:8000/analyze_defects" \
  -H "Content-Type: application/json" \
  -d '{"repo_id": "namespace/project", "max_issues": 5}'
```

### GET `/`

Проверка работоспособности сервера.

**Response:**
```json
{
  "message": "TestOps Copilot работает на 100%. Все режимы — включая оптимизацию и тест-план — готовы к защите!"
}
```

## 🏗️ Структура проекта

```
backend/
├── main.py                 # Главный файл FastAPI приложения
├── config.py              # Конфигурация и настройки
├── cloud_ru.py            # Интеграция с Cloud.ru Evolution API
├── gitlab_client.py       # Интеграция с GitLab API для коммита тестов
├── validator.py           # Валидатор Allure кода
├── openapi_parser.py      # Парсер OpenAPI спецификации
├── logging_config.py      # Настройка логирования
├── requirements.txt       # Зависимости Python
├── prompts/               # Шаблоны промптов для LLM
│   ├── auto_api.txt
│   ├── auto_ui.txt
│   ├── manual_api.txt
│   ├── manual_ui.txt
│   ├── test_plan.txt
│   ├── optimize.txt
│   └── unit_ci.txt
├── openapi/               # OpenAPI спецификации
│   └── openapi-v3.yaml
└── tests/                 # Тесты
    ├── test_main.py
    ├── test_cloud_ru.py
    └── ...
```

## 🧪 Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=. --cov-report=html

# Текущий показатель покрытия: ~90%

# Конкретный тест
pytest tests/test_main.py
```

## 📝 Логирование

Логи сохраняются в структурированном формате с информацией о:
- Входящих запросах (метод, URL, IP)
- Завершенных запросах (статус, время выполнения)
- Ошибках генерации

## ⚙️ Конфигурация

Все настройки находятся в `config.py` и загружаются из переменных окружения:

- `CLOUD_RU_API_KEY` - API ключ Cloud.ru (обязательно)
- `CLOUD_RU_MODEL` - Модель LLM (по умолчанию: `Qwen/Qwen3-Next-80B-A3B-Instruct`)
- `GITLAB_URL` - URL GitLab instance (опционально, для коммита тестов)
- `GITLAB_TOKEN` - GitLab токен с правами `api` и `write_repository` (опционально)

## 🔒 CORS

Для разработки CORS настроен на разрешение всех источников (`allow_origins=["*"]`). В продакшене рекомендуется ограничить разрешенные домены.

## 📸 Скриншоты

Скриншоты работы приложения доступны в папке `../photo/`:
- Интерфейс приложения
- Примеры сгенерированных тестов
- Результаты валидации

