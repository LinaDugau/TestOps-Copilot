# TestOps Copilot - Frontend

Frontend-часть приложения TestOps Copilot - веб-интерфейс для генерации тестов.

## 📋 Описание

React-приложение с современным интерфейсом на Material-UI для взаимодействия с TestOps Copilot API. Позволяет генерировать тесты различных типов, просматривать результаты, копировать код и оптимизировать существующие тесты.

## 🚀 Установка и запуск

### Требования

- Node.js 16+ (рекомендуется 18+)
- npm или yarn
- Запущенный backend сервер на `http://localhost:8000`

### Шаг 1: Установка зависимостей

```bash
cd frontend

# Установка пакетов
npm install
```

### Шаг 2: Запуск development сервера

```bash
npm start
```

Приложение откроется автоматически в браузере по адресу: `http://localhost:3000`

### Шаг 3: Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `build/`.

### Шаг 4: Запуск тестов

```bash
npm test
```

## 🎨 Функционал

### Режимы генерации

1. **Ручные тесты — UI Калькулятор** (`manual_ui`)
   - Генерирует 18+ ручных тест-кейсов для UI калькулятора
   - Тесты готовы для ручного выполнения

2. **Ручные тесты — API Evolution Compute** (`manual_api`)
   - Генерирует 18+ ручных тест-кейсов для Evolution Compute API
   - Включает позитивные и негативные сценарии

3. **Автоматические e2e UI тесты** (`auto_ui`)
   - Генерирует автоматические тесты на Playwright + pytest
   - Готовые к запуску e2e тесты

4. **Автоматические API тесты** (`auto_api`)
   - Генерирует автоматические тесты на pytest + requests
   - Использует реальную OpenAPI спецификацию
   - Все тесты соответствуют стандартам Allure

5. **Генератор тест-плана** (`test_plan`)
   - Анализирует ранее сгенерированный код
   - Создает структурированный тест-план

6. **Оптимизация тестов** (`optimize`)
   - Анализирует код на наличие дублей
   - Предлагает улучшения покрытия
   - Возвращает полный оптимизированный код

### Возможности интерфейса

- **Выбор типа генерации** - Dropdown с описанием каждого режима
- **Отображение результатов** - Подсветка синтаксиса кода
- **Валидация** - Автоматическая проверка соответствия стандартам Allure
- **Копирование кода** - Кнопка для копирования сгенерированного кода
- **Оптимизация** - Быстрый переход к оптимизации сгенерированных тестов
- **Обработка ошибок** - Понятные сообщения об ошибках

## 🏗️ Структура проекта

```
frontend/
├── public/                 # Статические файлы
│   ├── index.html
│   └── ...
├── src/
│   ├── App.tsx            # Главный компонент приложения
│   ├── App.css            # Стили приложения
│   ├── index.tsx          # Точка входа
│   └── ...
├── package.json           # Зависимости и скрипты
└── tsconfig.json          # Конфигурация TypeScript
```

## 🔌 Интеграция с Backend

Frontend отправляет POST запросы на `http://localhost:8000/generate` с телом:

```typescript
{
  type: string,              // Тип генерации
  previous_code?: string     // Код для анализа (для optimize/test_plan)
}
```

### Настройка API URL

По умолчанию используется `http://localhost:8000`. Для изменения адреса backend сервера отредактируйте `src/App.tsx`:

```typescript
const res = await axios.post('http://your-backend-url:port/generate', payload);
```

## 🎨 Технологии

- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Material-UI (MUI)** - Компоненты интерфейса
- **Axios** - HTTP клиент
- **react-syntax-highlighter** - Подсветка синтаксиса кода

## 📦 Основные зависимости

```json
{
  "react": "^19.2.1",
  "@mui/material": "^7.3.6",
  "axios": "^1.13.2",
  "react-syntax-highlighter": "^16.1.0",
  "typescript": "^4.9.5"
}
```

## 🖼️ Скриншоты

Скриншоты работы приложения доступны в папке `../photo/`:

1. **Screenshot 2025-12-07 at 02-12-54 React App.png** - Главный экран приложения
2. **Screenshot 2025-12-07 at 02-14-04 React App.png** - Пример генерации тестов
3. **Screenshot 2025-12-07 at 02-14-35 React App.png** - Результаты валидации
4. **Screenshot 2025-12-07 at 02-15-34 React App.png** - Сгенерированный код
5. **Screenshot 2025-12-07 at 02-18-28 React App.png** - Режим оптимизации
6. **Screenshot 2025-12-07 at 02-19-22 React App.png** - Тест-план

## 🐛 Troubleshooting

### Ошибка подключения к backend

**Проблема:** `Network Error` или `Cannot connect to backend`

**Решение:**
1. Убедитесь, что backend сервер запущен на `http://localhost:8000`
2. Проверьте, что порт 8000 не занят другим приложением
3. Проверьте настройки CORS в backend

### Ошибка генерации тестов

**Проблема:** Получение ошибки от API

**Решение:**
1. Проверьте, что API ключ Cloud.ru настроен в backend `.env`
2. Убедитесь, что есть интернет-соединение
3. Проверьте логи backend сервера

### Проблемы с зависимостями

**Проблема:** Ошибки при `npm install`

**Решение:**
```bash
# Очистка кэша и переустановка
rm -rf node_modules package-lock.json
npm install
```

## 📝 Заметки для разработчиков

- Приложение использует функциональные компоненты React с хуками
- Все состояния управляются через `useState`
- Стилизация выполнена через Material-UI sx prop
- Цветовая схема: темная тема с акцентом #00ff9d

## 🚀 Production сборка

```bash
npm run build
```